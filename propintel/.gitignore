# Data files — repopulate via ingestion pipeline
data/**
!data/**/
!data/**/.gitkeep

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# DuckDB — derived artifact, rebuilt via dbt build
*.duckdb
*.duckdb.wal

# Environment
.env
